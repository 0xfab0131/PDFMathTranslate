services:
  pdf2zh_next_gui:
    container_name: pdf2zh-next-ollama-gui
    command:
      - pdf2zh
      - --gui
      - --config-file
      - /config/pdf2zh_next.ollama.toml
    extra_hosts:
      - "host.docker.internal:host-gateway"
